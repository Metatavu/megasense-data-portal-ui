import { createStyles } from "@material-ui/core";

export const styles = createStyles({
  routingForm: {
    marginLeft: "auto",
    marginRight: "auto"
  },
  routingFormInput: {
    margin: 10,
    backgroundColor: "white"
  }
});